---
name: agent-recovery
description: >
  Use when recovering from context/session/sandbox loss, creating checkpoints, or
  reconstructing task state from durable sources. Keywords: recovery, checkpoint,
  context loss, session loss, sandbox loss, durable, reconstruct.
---

canonical Skill は `../.agents/skills/agent-recovery/SKILL.md` である。発火時にその全文を読み、binding workflow として実行する。この adapter に手順を複製しない。
