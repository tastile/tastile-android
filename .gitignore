*.iml
.gradle/
.kotlin/
.tmp/
local.properties
.idea/
.cursor/
.vscode/
.DS_Store
build/
build.del2/
captures/
.externalNativeBuild/
.cxx/
app/src/main/jniLibs/
# Orphaned one-time verification artifacts from the 2026-07-23 lint sweep.
# These are not referenced by any design or audit doc and should be regenerated
# under .tmp/ if a fresh run is ever needed.
evidence/
*.so
*.apk
*.aab
*.jks
*.keystore
*.log
reference/
.build-logs/
.tools/
*.bak
keystore.properties
release-keystore.*
google-services.json
*.hprof
# Vendored OpenAPI spec — replaced by the cross-repo submodule at
# ../../openapi/openapi.yaml (gradle.properties: openapi.input). The build
# pipeline regenerates the Kotlin client from the submodule on every
# `:app:generateV1Api` run, so the file stays on disk but stays out of git.
# (See the plan ref in commit message for the rollback path.)
/app/openapi/v1.json
